import random
mylist = [random.randint(0, 9) for i in range(50)]

# 01. Sum all the elements of the list.
# 02. Write code that removes all the duplicates in a list.
# 03. Write code that finds the similarities
#     between two lists (Intersection)
# 04. Write code that finds the differences
#     between two lists.
# 05. Write code that gets the frequency of items in a list.